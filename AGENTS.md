# beyond-may-be AI 작업 규칙

## 적용 범위

- 이 규칙은 저장소 전체에서 Claude Code와 Codex가 함께 따른다.
- 사용자에게 보내는 설명은 기본적으로 한국어로 작성한다.
- 기존 코드 스타일을 먼저 따른다.
- 코드 주석과 애플리케이션 로그는 한국어로 작성한다.
- 비밀번호, API 키, 접근 토큰, 개인정보를 코드나 문서에 기록하지 않는다.
- 프로젝트 목적, 현재 상태, 구조, 실행·검증 방법과 보호 영역은 [프로젝트 README](README.md)에서 확인한다.

## API 계층 컨벤션

- `src/main/java`의 도메인별 API 계층을 새로 만들거나 수정할 때는 [API 계층 코딩 컨벤션](docs/api-layer-convention.md)을 따른다.
- 이 문서의 적용 대상은 Controller, Service, Converter, DTO이며, 기존 코드 스타일과 충돌하면 해당 도메인의 기존 스타일을 먼저 확인한다.

## 새로운 코드 변경 요청 판단

- 사용자가 새로 요청한 코드 변경만 큰 작업 또는 작은 작업으로 판단한다.
- 승인된 계획을 실행하는 중에는 작업 크기를 다시 판단하지 않는다.
- 먼저 관련 내용을 살펴보고 충분히 추론한 뒤 판단 이유를 설명한다.
- 다음 중 하나라도 해당하면 큰 작업으로 판단한다.
  - 여러 기능이나 구조에 영향을 준다.
  - 되돌리기 어렵다.
  - 잘못되었을 때 피해가 크다.
  - 상황을 살펴본 결과 설계가 필요하다.
- 영향 범위가 작고, 되돌리기 쉬우며, 위험이 낮으면 작은 작업으로 판단한다.

큰 작업이면 다음 형식으로 승인을 요청한다.

> 이 작업은 {이유}이므로 큰 작업으로 추정됩니다. `design-spec` 스킬을 실행할까요?

작은 작업이면 고칠 부분과 구현 방법을 짧게 포함하여 다음 형식으로 승인을 요청한다.

> 이 작업은 {이유}이므로 작은 작업으로 추정됩니다. {구현 방법}으로 바로 구현할까요?

- `승인`, `진행`, `구현해`처럼 뜻이 분명한 답변만 승인으로 인정한다.
- 사용자가 큰 작업 판단을 거절하면 작은 작업으로 처리할 방법을 설명하고 구현 승인을 요청한다.
- 아주 위험한 작업이면 경고와 함께 설계를 한 번 더 요청한다.
- 두 번째 설계 요청도 거절하면 작은 작업 절차로 진행하고 `.dev/logs/yymmdd-risk-decisions.md`에 거절 사실을 기록한다.
- 사용자가 작은 작업 판단을 거절하면 `design-spec` 실행을 요청한다.

## 큰 작업 흐름

다음 순서를 지킨다.

```text
큰 작업 판단
→ design-spec 실행 승인
→ 설계 문서 작성과 승인
→ plan 실행
→ 계획 문서 작성과 승인
→ implement 실행
→ 구현과 검증
→ 구현 문서 작성
```

- 각 승인을 받기 전에는 다음 단계로 넘어가지 않는다.
- 사용자가 `설계해줘` 또는 `design-spec을 실행해줘`라고 직접 요청하면 작업 크기 판단을 건너뛴다.
- 사용자가 `계획을 만들어줘` 또는 `plan을 실행해줘`라고 직접 요청하면 작업 크기 판단을 건너뛴다.
- 사용자가 특정 계획을 가리키며 `이 계획을 구현해줘`라고 말하면 계획 승인으로 보고 `implement`를 실행한다.

## 작은 작업 흐름

- 고칠 부분과 구현 방법을 짧게 설명하고 승인을 기다린다.
- 승인되면 별도의 `implement` 스킬 없이 바로 구현한다.
- 테스트가 필요한 코드이면 구현 전에 `TDD 방식으로 구현하겠습니다.`라고 알린다.
- TDD가 필요 없는 작업이면 구현 전에 `TDD 제외 작업입니다.`라고 알린다.
- 관련 테스트만 실행한다.
- 구현 문서를 만들지 않고 채팅으로 변경 내용과 테스트 결과를 설명한다.

## 구현 중 변경 처리

- 승인된 설계 안에서 현재 구현을 고치는 추가 요청은 새로운 작업으로 판단하지 않는다.
- 추가 요청이 설계의 목적이나 구조를 바꾸면 구현을 멈추고 설계 문서를 수정한다.
- 어느 쪽인지 분명하지 않으면 한 번에 하나씩 질문한다.
- 변수 이름이나 작은 코드 위치처럼 결과와 방법이 같은 차이는 스스로 처리한다.
- 중요한 구현 방법이 계획과 달라지면 구현을 멈추고 `현재 구현 방법이 승인된 계획과 달라집니다. 계획 문서를 업데이트할까요?`라고 질문한다.
- 계획 수정이 승인되면 계획을 업데이트하고 재승인받은 뒤 구현을 계속한다.
- 계획 수정이 거절되면 계획 문서를 바꾸지 않고 사용자가 요청한 구현을 계속한다.

## TDD와 검증

- 테스트가 필요한 코드 작업은 `실패하는 테스트 → 통과하는 최소 코드 → 코드 정리` 순서로 구현한다.
- 문서와 단순 설정처럼 테스트가 필요 없는 작업에는 TDD를 억지로 적용하지 않는다.
- 이번 변경 때문에 테스트가 실패하면 스스로 수정한다.
- 기존 문제로 테스트가 실패하면 원인을 구분해 보고한다.
- 환경 문제로 테스트나 빌드를 실행하지 못하면 코드 작업 완료 여부와 검증 실패 사유를 나누어 설명한다.
- 구현이 끝나면 변경한 코드를 다시 읽고 실수를 확인한다.

## 프로젝트 검증 명령

Windows에서는 다음 명령을 사용한다.

- 관련 테스트: `.\gradlew.bat test --tests "전체.테스트.클래스명"`
- 전체 테스트: `.\gradlew.bat test`
- 코드 품질 검사: `.\gradlew.bat spotlessCheck`
- 전체 빌드: `.\gradlew.bat build`

macOS와 Linux에서는 `gradlew.bat` 대신 `./gradlew`를 사용한다.

- 작은 작업은 관련 테스트만 실행한다.
- 큰 작업은 전체 테스트, 코드 품질 검사, 전체 빌드를 실행한다.

## 개인 작업 문서

- 개인 작업 문서는 Git에서 제외된 `.dev/` 아래에 저장한다.
- 설계 문서: `.dev/specs/yymmdd-nn-{작업 내용}-design.md`
- 계획 문서: `.dev/plan/yymmdd-nn-{작업 내용}-plan.md`
- 구현 문서: `.dev/implementation/yymmdd-nn-{작업 내용}-implementation.md`
- 같은 작업의 설계·계획·구현 문서는 같은 날짜와 작업 번호를 사용하고 서로 링크한다.
- 문서에는 상태를 표시하고 승인 시 날짜와 시간을 기록한다.
- 중요한 수정이 생기면 상태를 `작성 중`으로 되돌리고 다시 승인받는다.
- 변경 이력은 따로 쌓지 않고 최신 내용만 유지한다.

## Git과 외부 작업

- 커밋, 브랜치 생성·변경, push, Pull Request 생성은 사용자가 요청할 때만 실행한다.
- 커밋이나 Pull Request 요청을 받으면 하네스 업데이트가 필요한지 점검한다.
- 업데이트가 필요하면 내용을 설명하고 승인받은 뒤 수정한다.
- GitHub Wiki에 공유할 가치가 있는 개인 문서를 발견하면 이유를 설명하고 게시 승인을 요청한다.
- 승인되면 개인·비밀 정보를 제거한 팀용 문서로 다시 작성해 게시한다.
- 같은 주제의 Wiki 문서가 있으면 수정할지 새로 만들지 질문한다.
- 데이터 삭제처럼 되돌리기 어려운 작업은 승인된 계획에 있어도 실행 직전에 다시 확인한다.
- Git으로 되돌릴 수 있는 코드 파일 삭제는 승인된 계획에 포함되어 있으면 다시 묻지 않는다.

## 완료 보고

- 바꾼 내용, 실행한 검사, 검사 결과, 남은 문제를 구분해서 설명한다.
- 실행하지 못한 검사가 있으면 생략하지 말고 이유를 적는다.
- 하네스와 관계없는 사용자 변경을 수정하거나 되돌리지 않는다.

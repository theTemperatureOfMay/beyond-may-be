interface:
  display_name: "Triage"
  short_description: "로컬 ticket 상태·blocker·frontier를 판정"
  default_prompt: "Use $triage to assess this local Markdown ticket, its blockers, and the current frontier."
policy:
  allow_implicit_invocation: true

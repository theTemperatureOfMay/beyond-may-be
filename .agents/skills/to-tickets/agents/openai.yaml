interface:
  display_name: "To Tickets"
  short_description: "승인된 spec을 blocker가 있는 구현 tickets로 분할"
  default_prompt: "Use $to-tickets to draft local Markdown implementation tickets and blockers from the approved spec."
policy:
  allow_implicit_invocation: true

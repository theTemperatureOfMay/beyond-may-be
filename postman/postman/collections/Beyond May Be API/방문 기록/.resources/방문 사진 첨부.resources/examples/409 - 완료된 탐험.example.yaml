$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/visits/9001/photos"
  method: POST
  headers:
    Authorization: Bearer {{accessToken}}
response:
  statusCode: 409
  statusText: Conflict
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "이미 완료된 탐험입니다.",
        "code": "EXPLORATION409_2",
        "data": null,
        "success": false
      }
order: 3000

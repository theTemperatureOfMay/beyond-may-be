$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/courses/1/join"
  method: POST
  headers:
    Authorization: Bearer {{accessToken}}
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "성공입니다.",
        "code": "COMMON200",
        "data": {
          "explorationId": 1,
          "participantId": 2,
          "displayName": "여행자 (2)",
          "role": "MEMBER"
        },
        "success": true
      }
order: 1000

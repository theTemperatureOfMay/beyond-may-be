$kind: http-request
description: "탐험 전·진행 중 현재 팀원과 참여자별 고유 방문 장소 수를 조회합니다(4.2.2, 4.3.2). 인증 필요: `Authorization: Bearer {{accessToken}}`. Path variable `explorationId`; query와 요청 본문은 없습니다. `LEFT` 참여자는 제외하고 OWNER 우선·합류 시각 오름차순으로 반환하며 사용자 ID와 실시간 위치 좌표는 노출하지 않습니다."
url: "{{baseUrl}}/api/v1/explorations/44/participants"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
examples: ./.resources/팀원 목록 조회.resources/examples
order: 4000

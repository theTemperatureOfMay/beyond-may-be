$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/courses"
  method: GET
  headers:
    Authorization: Bearer {{accessToken}}
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |
      {
        "message": "성공입니다.",
        "code": "COMMON200",
        "data": {
          "courses": []
        },
        "success": true
      }
order: 2000

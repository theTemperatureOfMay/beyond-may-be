@AGENTS.md

# Claude Code 연결 규칙

- 프로젝트 스킬은 `.claude/skills/`에서 찾는다.
- 각 Claude Code 스킬은 `.agents/skills/`에 있는 같은 이름의 원본 스킬을 가리킨다.
- 스킬을 실행할 때 연결 파일이 지정한 원본 `SKILL.md`를 끝까지 읽고 그대로 따른다.
- 공통 행동 규칙의 원본은 `AGENTS.md`이며 이 파일에 같은 규칙을 복사하지 않는다.
